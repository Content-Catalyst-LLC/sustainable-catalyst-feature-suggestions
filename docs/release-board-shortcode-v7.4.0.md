# `[sc_release_board]` v7.4.0

The shortcode and all existing attributes remain compatible:

`layout`, `context`, `groups`, `products`, `limit`, `show_status`, `show_updated`, `show_links`, `show_header`, `show_footer`, `show_source`, `dense`, `inactive`, `title`, `heading_level`, `interval`, and `rotate`.

The default terminal layout continues to rotate Foundation, Research and Intelligence, Data and Analysis, Creation and Systems, and Commercial Release. Registry `console_screen` assignments now control placement without changing each product's broader family.
