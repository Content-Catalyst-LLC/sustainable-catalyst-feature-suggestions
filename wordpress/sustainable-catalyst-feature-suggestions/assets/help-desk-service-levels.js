(function(){'use strict';document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('[data-scfs-service-levels] select[name="operation"]').forEach(function(select){var form=select.closest('form');if(!form){return;}form.addEventListener('submit',function(event){var operation=select.value;if((operation==='pause'||operation==='resume')&&!form.querySelector('input[name="reason"]').value.trim()){event.preventDefault();form.querySelector('input[name="reason"]').focus();window.alert('Add a reason before changing the service clock state.');}});});});}());
